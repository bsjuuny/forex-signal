# 💵 Forex Signal (실시간 환율 감시) 개발자 가이드

안녕하세요! 이 프로젝트는 환율의 미세한 움직임을 감시하다가, 우리가 설정한 범위를 벗어나면 즉시 텔레그램으로 알려주는 **환율 파수꾼** 서비스입니다.

---

## 🏗 시스템 구성 (Workflow)

1.  **데이터 수집**: `scripts/collect-data.ts`가 한국수출입은행 API를 호출해 주요 통화(USD, JPY, EUR 등)의 현재가를 가져옵니다.
2.  **데이터 가시화**: Next.js 기반 대시보드에서 `lightweight-charts`를 사용해 과거 대비 환율 흐름을 예쁘게 그려줍니다.
3.  **알림**: 현재가가 목표가에 도달하거나 급변동이 감지되면 텔레그램 봇이 메시지를 발송합니다.

---

## 🛠 주요 명령어 및 실행 가이드

### 1. 환율 업데이트 및 알림
- `npm run data:update`: 지금 바로 환율 정보를 새로고침하고 싶을 때 실행합니다.
- `npm run notify`: 현재 환율 상황을 텔레그램 리포트로 만들어서 보냅니다.

### 2. 로컬 개발
- `npm run dev`: 환율 차트 화면을 수정하거나 데이터 현황을 웹으로 보고 싶을 때 사용합니다.

---

## 💡 개발 시 유의사항 (유지보수)

### API 호출 제한에 주의하세요
- 한국수출입은행 API는 무료이지만 일간/시간당 호출 횟수 제한이 있습니다. 그래서 우리는 스케줄러(`scheduler`)에서 매 시 45분에 한 번씩만 호출하도록 설정해 두었습니다.

### 차트 디자인 수정
- 만약 차트의 색상이나 폰트를 바꾸고 싶다면 `src/app/page.tsx` 내부의 `lightweight-charts` 설정 코드를 건드리면 됩니다.

---

## 🚨 문제 해결 (Troubleshooting)

**Q: 데이터가 '0'으로 나오거나 응답이 없어요.**
- A: 수출입은행 API는 **평일 은행 영업시간** 위주로 운영됩니다. 밤늦게나 주말에는 데이터를 주지 않을 수 있으니, `scripts/collect-data.ts`에서 에러 로그를 확인해 보세요.

**Q: GitHub Pages에 업데이트가 안 돼요.**
- A: 스케줄러가 자동 빌드 후 `docs/` 폴더를 푸시하는 과정에서 `GITHUB_TOKEN` 권한 문제가 생겼을 수 있습니다.

---

## 📁 주요 폴더 설명
- `scripts/`: 환율 수집 및 텔레그램 발송 핵심 로직.
- `src/app/`: 환율 현황판 UI (Next.js 16 기반).
